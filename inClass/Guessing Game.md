Do not make any meta remarks, do not comment on how you will respond to my prompt. Do not write code unless specifically instructed.

1. choose a random number between 1 and 100 (the numbers 1 and 100 are also options), then plac ethat number into a variable called `number`
2. prompt the user to guess a random number between 1 and 100. Place their response into a variable called `guess`
3. if `guess` = `number`, then congratulate the user and tell them they are correct. Go back to number 1 and tell the user that you are starting a new game.
4. if `guess` < `number`, tell the user that their guess is too low, then go back to number 2.
5. if `guess` > `number`, tell the user that their guess is too high, then go back to number 2.