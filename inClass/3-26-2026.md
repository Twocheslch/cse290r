Prompt the user for an image. Then pause

One th euser submits and image, place the image into `original`. Please give me a summary of `original`, such as 
* where it is located
* how safe of an area it is for human children
* and how common a place like this could be found in South Idaho.

determine a collection of outdoor activities which would be appropraite to perform in the setting of `original`. Place these activities in the variable `activites`. Please then sort `activities` in order of most popular is first/at the top, and the rest follow suit in descending order. Then present `activities`.

Then, select the most popular item from among `activities` and place it into the variable `activity`. Then, do a web search to find out what equipment is required to perform `activity`. Place the most expensive, the most reasonable or the most value oriented option, and the cheapest option for each piece of equipment into a table. Place this table into the variable `table`. Present `table`.

After you present `table`, create a .json file named `cost_breakdown` and place `table` into it. 









Then, generate a python program to: 
1. compute the cost of all the most expensive items combined, and place that value into `maxcost`.
2. compute the cost of all the value items combined, and place that value into `midcost`.
3. compute the cost of all the least expensive items combined, and place that value into `lowcost`.

Please tell me the `maxcost`, `midcost`, and `lowcost`.








Using `midcost` and `maxcost`, use `original` as a background, and generate an advertisemnt with it, advertising the activity and the prices.